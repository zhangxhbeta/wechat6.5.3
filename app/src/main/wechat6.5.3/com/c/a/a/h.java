package com.c.a.a;

import java.io.UnsupportedEncodingException;

public final class h {
    static final int aHN = 5;
    static final int aHO = 7;
    static final byte[] aHP;
    static final int aHQ = 4;
    static final int aHR = 6;
    static final byte[] aHS;
    static final byte[] aHT = new byte[]{(byte) -75, (byte) -5, (byte) -12, (byte) 112, (byte) -40, (byte) -44, (byte) -7, (byte) -71};
    static final String aHU = ("/" + y.PROTOCOL + "/c.php");
    static final String aHV = ("/" + y.PROTOCOL + "/d.php");
    static final String aHW = ("_" + y.aNt + "_list.sensewhere-sdk.com");

    static {
        byte[] bytes;
        try {
            bytes = "$UEC,".getBytes("UTF-8");
        } catch (UnsupportedEncodingException e) {
            bytes = "$UEC,".getBytes();
        }
        aHP = bytes;
        try {
            bytes = "$UP,".getBytes("UTF-8");
        } catch (UnsupportedEncodingException e2) {
            bytes = "$UP,".getBytes();
        }
        aHS = bytes;
    }
}
